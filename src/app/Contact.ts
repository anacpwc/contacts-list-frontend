export interface Contact {
    id: number,
    name: string,
    number: string,
    email : string,
    category: string,
    isFavorite: boolean,
    city: string,
    occupation: string,
    birthDate: string,
    emergency: boolean
}